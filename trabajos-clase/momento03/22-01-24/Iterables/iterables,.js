'use strict'
//ITERADOR DE SIMBOLO

let range = {
    from: 1,
    to: 5
  };
  
  //Muestra desde el numero que pongamos hasta un rango que nosotros definimos

  let word = 'Hola Mundo'
for (let char of word) {
    alert( char ); 
}